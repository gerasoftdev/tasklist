{
  "name": "@repo/typescript-config",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean:deep": "pnpm run clean:deps",
    "clean:deps": "rm -rf node_modules",
    "lint": "eslint . --cache",
    "lint:fix": "eslint . --cache --fix"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:"
  }
}
